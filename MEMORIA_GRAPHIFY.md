# Memoria Graphify

Este workspace debe quedar alineado al mapa runtime de Graphify para el contenedor `smarteros-workspace`: `app/`, `components/`, `lib/`, `mcp/`, `middleware.ts`, `supabase/` y archivos Docker. El SaaS local usa Supabase Auth; no deben entrar productos externos, documentos historicos ni artefactos de analisis al build del contenedor.
